# Optional. If set, all /api/* and /lot/* endpoints require header: X-API-Key: <value>
# Leave unset for an open demo API (recommended for hackathon judging - one less thing to break).
MOCK_ERP_API_KEY=
